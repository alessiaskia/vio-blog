---
import { getCollection } from 'astro:content';
const posts = await getCollection('blog'); // "blog" is your content collection
---

<h1>Actualités</h1>
<ul>
  {posts.map(post => (
    <li>
      <a href={`/actu/${post.slug}`}>{post.data.title}</a>
    </li>
  ))}
</ul>